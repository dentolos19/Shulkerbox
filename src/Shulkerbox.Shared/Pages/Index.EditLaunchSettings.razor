@using Shulkerbox.Shared.Objects
<MudDialog>
    <DialogContent>
        <MudElement Class="d-flex flex-column gap-2">
            <MudSelect T="MinecraftAccount" Label="Account" @bind-Value="Account">
                @foreach (var account in UserAccounts)
                {
                    <MudSelectItem Value="account" />
                }
            </MudSelect>
            <MudSelect T="MinecraftVersion" Label="Version" @bind-Value="Version">
                @foreach (var version in GameVersions)
                {
                    <MudSelectItem Value="version" />
                }
            </MudSelect>
        </MudElement>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="Save">Save</MudButton>
    </DialogActions>
</MudDialog>