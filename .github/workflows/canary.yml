name: Canary Deployment
run-name: "${{ github.workflow }} #${{ github.run_number }}"
on:
  push:
    paths:
      - src/**
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true
env:
  TARGET_FILE: Shulkerbox.sln
jobs:
  build:
    name: Build
    runs-on: windows-latest
    steps:
      - name: Checkout Branch
        uses: actions/checkout@v3
        with:
          lfs: true
          submodules: recursive
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 7.0.x
      - name: Publish App
        run: |
          dotnet publish --configuration Release --output build
          $artifacts = (Get-ChildItem -Path build/*).FullName
          New-Item -ItemType Directory -Path artifacts
          Compress-Archive -Path $artifacts -DestinationPath artifacts/${{ github.event.repository.name }}.zip
      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: Output
          path: artifacts
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v3
        with:
          name: Output
          path: artifacts
      - name: Delete Preexisiting Release
        uses: dev-drprasad/delete-tag-and-release@master
        with:
          tag_name: canary
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload Release
        uses: softprops/action-gh-release@master
        with:
          body: This is a prerelease. Please be warned that this is not a stable release and may contain bugs.
          prerelease: true
          files: artifacts/*
          name: Canary
          tag_name: canary